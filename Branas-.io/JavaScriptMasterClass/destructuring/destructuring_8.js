const language = {
    name: "C",
    author: "Dennis Ritchie",
    year: 1972
}
const {name, author, year} = language;
//{} foi usado para referênciar multiplas variáveis para um mesmo objeto.
console.log(name, author, year);
