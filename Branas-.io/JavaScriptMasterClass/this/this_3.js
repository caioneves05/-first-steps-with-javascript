const calculateArea = function() {
    return this.x * this.y
}
const retangule = {
    x: 5,
    y: 2,
    calculateArea
    //A função deve ser chamada dentro da variável a qual será usada.
}
console.log(retangule.calculateArea())